# Tasks

## 1. The pure question module

- [x] 1.1 Create `src/tether/ask.lua` with `normalize(args)` implementing the `ask` spec's question shape and bounds: drop a question without usable text, default a missing `id` and de-duplicate ids, drop options whose `label` is missing/empty/non-string, coerce a non-boolean `multi` to false, ignore an out-of-range `recommended`, cap the call at 8 questions and a question at 12 options, and truncate question text to 1000 and a description to 8000 bytes; a question left with no usable option stays answerable through the freeform row alone; verify with unit cases in `tests/lua_tests.lua` (several questions in order, 9 questions → 8, 13 options → 12, empty options list, duplicate ids, unusable text, oversized text) and `luac -p src/tether/ask.lua`
- [x] 1.2 Add `encode(questions, answers)`, `summary(questions, answers)` and the cancellation payload to the module: the answers array in question order with `selected` always an array of verbatim labels, `other` and `notes` omitted when empty, notes returned for unselected options too, and the freeform-only case yielding `selected = {}` with `other` set; build it through `provider_common.json_encode` using the `_array` marker so empty lists encode as `[]`; verify with unit cases that the payload is single-line valid JSON, that it round-trips through `json_decode`, that an empty selection is `[]` and not `{}`, and that the summary names each question id with its selection; verify `lua tests/lua_tests.lua` passes
- [x] 1.3 Wire the module into the build: add `src/tether/ask.lua` to `LUA_MODS`, add `ask_lua src/tether/ask.lua` to the `tools/embed.lua` argument list, add the `luac -p` line to the `test` target, and register it as a global in `src/host/main.c` before `agent`; verify `make` builds, `grep -c ask_lua src/host/embed.c` is non-zero, and `./tether --version` prints the version (task 2.1 depends on the global being registered)

## 2. Agent: park the turn, record the answer

- [x] 2.1 Add the `ask` line to the built-in tool description in both `agent.builtin_prompt` and `context.BUILTIN_PROMPT`, and add a test asserting both listings name the same tools so the copies cannot drift; verify `lua tests/lua_tests.lua` passes and `sh tests/context_e2e.sh ./tether` still finds its expected prompt
- [x] 2.2 Add the `ask` branch to `agent.drive_pending`: a call named `ask` is never executed, marks itself emitted once, emits one `ask` event carrying the call id and the `ask.normalize` result, and parks the turn with the calls behind it left pending; verify with agent-level cases that one event is emitted, that a repeated `agent.continue`/queue drive does not re-emit it, that a `read` queued after the `ask` does not run while it is parked, and that `luac -p src/tether/agent.lua` passes
- [x] 2.3 Add `M.answer_ask(id, answer, cfg, on_event)`: it records `ask.encode(...)` as the call's tool result, appends it to history and journals it, marks the call done, drives the queue, and returns whether another interaction is pending; verify with a case that answers a parked call, then resumes with `agent.continue`, and asserts the `tool` history message carries the payload, that the journal received a `tool_result` event, and that the resumed request sees the answer
- [x] 2.4 Make cancellation resolve the batch: `M.answer_ask(id, { cancelled = true }, ...)` marks every still-pending `ask` call done with the cancellation payload while a pending non-`ask` call still runs on the normal path; verify with a case that queues two `ask` calls and a `write`, cancels the first, and asserts both cancels, no second question event, and the `write` executed
- [x] 2.5 Handle non-interactive runs: set `cfg.non_interactive = true` in `app.lua`'s print path and make the `ask` branch of `drive_pending` produce an error tool result that names the mode instead of parking or emitting an event; verify with an agent case that a turn calling `ask` with the flag set gets the error result, makes its next request, and emits no `ask` event, and with a manual `--print` run against a stub that calls `ask`

## 3. TUI: the question block

- [x] 3.1 Add the interaction state (`S.ask` with `questions`, `qidx`, `sel`, mode, editor buffers, per-question answers/notes) and render it as a synthetic tail entry: extend `sync_tail` with an `ask_entry` case, add the `virt = "ask"` branch to `render_entry` (question row with `N/M` progress when the call has more than one question, markdown-lite description above the options, indexed option rows with the highlighted row reversed, a recommended marker, `[x]`/`[ ]` toggles on a multi question, dim note lines under their option, and a trailing freeform row labelled `Other (ввести свой вариант)`); verify with a UI case that drives an `ask` event and asserts the rendered rows (question text, indices, progress, recommended marker, freeform row) via `_render_all`
- [x] 3.2 Handle the block's keys in `list` mode by checking `S.ask` in `handle_key` before the input path: `↑`/`↓` move across the option rows and the freeform row, `Enter` submits a single-answer question (or accepts a `multi` question and advances), a digit `1..9` submits on a single-answer question and toggles on a `multi` question, `Space` toggles on a `multi` question, `Enter` on the freeform row opens the freeform editor when the question has no committed freeform text and submits/advances once it has (the freeform-only submission path agreed while implementing, spec tui "A freeform-only answer can be submitted"), `Tab` opens the note editor on an option row (and does nothing on the freeform row), `←` returns to the previous question with its state intact, `Esc` cancels the set, and any other key changes nothing; verify with scripted-key cases (arrow+`Enter`, digit, `Space` toggle, `←` restore, unused letter leaves the input line and the turn untouched) plus a case asserting the placeholder/caret are cleared while the block is open
- [x] 3.3 Implement the two editors: `Enter` on the freeform row opens it prefilled with the current freeform answer and `Tab` on an option opens that option's note editor, both single-line inside the block; characters and backspace edit the buffer without moving the highlight, `Enter` commits (an empty commit clearing the value) and returns to the list, and `Esc` discards the edits made in that editor and returns to the list without cancelling the set; verify with scripted-key cases for a typed freeform answer reaching the payload, a note shown under its option and returned with the answer, a note discarded by `Esc`, and `↑` in an editor not moving the highlight
- [x] 3.4 Implement submission and cancellation: on the last question `Enter` removes the block, appends one dim row with `ask.summary(...)`, records the answers through `agent.answer_ask`, and resumes the turn with the busy/waiting/paint sequence the confirmation path uses; `Esc` in the list removes the block, appends a dim cancellation row, records the cancellation, and resumes the turn with no error banner; verify with UI cases for a two-question submit (block gone, one summary row, turn resumed) and for a cancel (block gone, cancellation row, no banner), plus an ASCII-mode case asserting every glyph the block introduces degrades
- [x] 3.5 Document the block's keys in the `KEYMAP` table's neighbourhood if a section documents bindings, and confirm the block introduces no non-ASCII glyph outside `GLYPH_MAP`; verify `M.to_ascii` maps every new glyph used by the block (note marker, progress, recommended hint) and `luac -p src/tether/ui.lua` passes

## 4. Documentation

- [x] 4.1 Document the `ask` tool in `README.md`: it in the tool list the agent prompt describes, the question block's keys, and the `--print` behaviour (the model is told no interactive user is available); verify the documented label and keys match `ask.lua`'s constants and the UI handler
- [x] 4.2 Update `docs/tech-spec.md`: the data-flow section gains the `ask` event and the parked-turn answer path, and the key-decision list records that `ask` rides the confirmation queue, that the question rules live in `ask.lua`, and that `--print` degrades to an error result; verify the documented event names match the emitted events

## 5. Verification

- [x] 5.1 Run `make test` and confirm `luac -p`, `tests/lua_tests.lua`, `tests/context_tests.lua`, `tests/context_e2e.sh`, `tests/host_smoke.sh` and the host-primitives test all pass with the new module embedded
- [x] 5.2 Smoke-test by hand: run `./tether` against a provider (or a stub) whose model calls `ask` with two questions, one `multi` with a `recommended` option and a description, and confirm the block renders, the arrows/digits/`Space`/`Tab`/`Enter` flow works, a note and a freeform answer reach the model's next request, `←` restores an earlier answer, `Esc` cancels without an error banner and the turn continues, and the decision row appears; then run `--print` on the same prompt and confirm the model gets the non-interactive error result and the run still prints its answer
- [x] 5.3 Run `openspec validate add-ask-tool --strict` and confirm the change is valid
