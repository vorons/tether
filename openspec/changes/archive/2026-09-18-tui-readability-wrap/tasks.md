# Tasks

## 1. Перенос прозы по словам

- [x] 1.1 Перевести `wrap()` в `src/tether/ui.lua` на ширину в колонках (`vlen`/`char_width`), разрыв по пробелам, токены без пробелов — жёсткий рез; проверить тестом переноса длинного предложения без разрывов внутри слов
- [x] 1.2 Покрыть ширину CJK/emoji (2 колонки) и SGR (0) в переносе; проверить тестами `vlen`-кейсов в `tests/lua_tests.lua`

## 2. Мягкий перенос код-блоков

- [x] 2.1 Врапить строки код-блоков внутри рамки `md_render()` с отступом продолжения вместо обрезки с маркером; проверить тестом: длинная строка даёт несколько строк рамки, контент цел, маркера нет
- [x] 2.2 Считать ширину префиксов списков через `vlen` (пуля `•`); проверить тестом выравнивания врапнутых пунктов

## 3. Регрессия и приёмка

- [x] 3.1 Сохранить поведение `ui.wrap = false` (обрезка с маркером в одну строку); проверить существующим тестом T31
- [x] 3.2 Прогнать полный `tests/lua_tests.lua` и убедиться, что все существующие проверки TUI (T31/T32/T39/T40/T45) зелёные
